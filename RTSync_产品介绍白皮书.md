# RTSync 产品介绍白皮书

**企业级异构数据库实时同步平台**

---

版本：v2.14 | 发布日期：2026年3月 | 厂商：1stBlue

---

## 目录

1. [产品概述](#1-产品概述)
2. [行业背景与市场需求](#2-行业背景与市场需求)
3. [核心功能](#3-核心功能)
4. [技术架构](#4-技术架构)
5. [支持的数据库与连接器](#5-支持的数据库与连接器)
6. [典型应用场景](#6-典型应用场景)
7. [性能指标与基准测试](#7-性能指标与基准测试)
8. [运维与监控体系](#8-运维与监控体系)
9. [安全与合规](#9-安全与合规)
10. [部署架构](#10-部署架构)
11. [版本演进路线](#11-版本演进路线)
12. [总结](#12-总结)

---

## 1. 产品概述

RTSync 是由 1stBlue 公司自主研发的**企业级异构数据库实时数据同步平台**，专注于解决企业在多数据库环境下的数据迁移、实时同步与一致性保障问题。

RTSync 提供了一套完整的数据同步解决方案，支持多种数据源之间的实时数据迁移和同步。通过简单的可视化配置，即可实现数据的可靠传输和一致性保证。

### 1.1 产品定位

RTSync 定位为企业级数据基础设施中间件，面向以下核心场景：

- **异构数据库迁移**：在不同厂商、不同架构的数据库之间进行无缝数据迁移
- **实时数据同步**：基于 CDC 技术实现毫秒级数据变更同步
- **国产化替代**：深度适配国产数据库生态，支撑信创工程落地
- **数据一致性保障**：内置数据比对引擎，确保源端与目标端数据完全一致

### 1.2 核心优势

| 优势维度 | 描述 |
|---------|------|
| **广泛的数据库覆盖** | 支持 23+ 种数据库连接器，覆盖主流商业、开源及国产数据库 |
| **流批融合架构** | 全量迁移与增量同步无缝衔接，统一引擎、统一管理 |
| **高性能** | 流式复制架构经优化后性能提升约 60%，支持并行读写 |
| **可视化操作** | 提供现代化 Web 控制台，全程可视化配置与管理 |
| **数据一致性** | 内置自动比对与全文比对双重校验机制 |
| **高可用** | 支持故障转移、负载均衡和断点续传 |

---

## 2. 行业背景与市场需求

### 2.1 数字化转型驱动

随着企业数字化转型深入推进，异构数据库并存的局面日益普遍。企业 IT 系统中往往同时运行 Oracle、MySQL、PostgreSQL、SQL Server 等多种数据库，数据孤岛问题日趋严重，对统一的数据同步基础设施提出了迫切需求。

### 2.2 信创国产化浪潮

中国信创产业快速发展，大量企业和政府机构正在将核心系统从 Oracle、SQL Server 等商业数据库迁移至达梦、人大金仓、GBase、GaussDB、OceanBase 等国产数据库。这一迁移过程对数据同步工具的异构兼容性、数据完整性和迁移效率提出了极高要求。

### 2.3 实时数据需求

金融、电信、制造等行业对数据的实时性要求越来越高。传统的 ETL 批处理模式已无法满足实时风控、实时报表、实时数据仓库等场景的需求，基于 CDC 的实时数据同步成为刚需。

---

## 3. 核心功能

### 3.1 连接管理

RTSync 提供统一的连接管理界面，支持对多种数据源进行集中配置和管理。

**核心能力：**
- 可视化连接配置：通过 Web 界面填写主机地址、端口、用户名、密码等信息即可完成连接创建
- 连接状态实时监测：实时展示每个连接的健康状态和详细信息
- 多连接集中管理：在统一界面中管理所有数据源连接

### 3.2 全量迁移（Batch）

通过数据库原生快照机制（如 MySQL 的 FLUSH TABLES WITH READ LOCK、Oracle 的 Flashback 等）完全拷贝源库所有数据，包括表结构和数据。

**核心能力：**
- **单表全量同步**：针对指定表执行全量数据复制
- **多表批量同步**：勾选多个目标表，一键触发批量全量同步
- **整库全量同步**：对作业内所有数据表执行全量同步，实现整库级别的数据迁移
- **实时状态监控**：展示同步运行状态、运行时间、数据量、同步速率等详细信息
- **任务控制**：支持单表或多表的同步任务启停控制

**典型应用：** 数据库迁移（如 Oracle → MySQL）、测试环境初始化、年结数据归档。

### 3.3 实时复制（Stream / CDC）

基于 **CDC**（Change Data Capture）变更数据捕获技术，通过解析数据库**事务日志**（如 MySQL 的 Binlog、Oracle 的 Redo Log）实时捕获所有 DML 变更，以毫秒级延迟同步到目标端数据库，保持事务原子性和顺序。

**核心能力：**
- **日志级实时捕获**：通过解析数据库事务日志捕获增量变更，对源库几乎零影响
- **毫秒级同步延迟**：变更数据以极低延迟传输至目标端
- **事务一致性保证**：保持事务原子性和顺序，确保数据一致
- **CDC 位点管理**：实时展示日志读取位点时间和数据写入时间
- **断点续传**：任何原因导致同步中断后，恢复时自动从记录的位点继续执行
- **CDC 起始点位设置**：支持自定义增量复制的起始位点

**典型应用：** 金融交易系统实时备份、微服务架构下的数据一致性同步、实时数据仓库构建。

### 3.4 数据比对

内置数据一致性校验引擎，提供两种互补的比对方式，确保数据同步的准确性和完整性。

#### 3.4.1 自动比对（基于统计信息）

- **触发方式**：自动/定时执行
- **核心原理**：在源端和目标端分别执行聚合查询（COUNT、SUM），比对统计结果
- **准确性**：高，能发现绝大多数数据丢失或数值错误
- **效率**：高，仅执行轻量级聚合查询，对数据库资源消耗极小
- **适用场景**：日常巡检、快速发现数据异常

#### 3.4.2 全文比对（基于 MD5 哈希）

- **触发方式**：人工触发
- **核心原理**：对每行数据进行全字段拼接并计算 MD5 哈希，逐行对比指纹
- **准确性**：100%，任何字段（包括单 bit）的差异都会被检测到
- **效率**：中等，需读取全量数据并进行哈希计算
- **适用场景**：故障排查、精细验证

### 3.5 作业管理

RTSync 以"作业"为核心管理单元，一个作业定义了从源端到目标端的完整数据同步流程。

**核心能力：**
- **向导式创建**：分步引导用户选择目标库、来源库、同步对象和配置参数
- **灵活的同步对象选择**：支持单表、多表、整库选择，支持批量导入（合并导入/覆盖导入）
- **三种复制模式**：
  - 不启用增量（手动全量复制）
  - 定时全量复制（按天/小时自动执行）
  - CDC 实时复制（基于日志的实时增量同步）
- **大小写策略**：支持保持原样、转为小写、转为大写三种模式
- **脏数据处理**：
  - 严格模式（Strict Mode）：遇到异常数据立即报错
  - 兼容模式（Compatible Mode）：丢弃异常行并继续处理
- **Alter Table 支持**：源端表结构变更时的自动处理策略
- **作业日志**：支持按对象名合并展示，显示最新一条日志记录

### 3.6 同步对象管理

支持对作业中的同步表进行精细化管理，包括表的批量编辑、附加列配置等高级操作。

### 3.7 监控与告警

#### 3.7.1 监控体系

RTSync 构建了完善的监控体系，核心理念为"雁过留声"——每次发现的异常都会产生监控事件并被完整记录，且全程跟踪每个监控事件的生命周期。

**监视器列表：**

| 监视器 | 功能描述 | 状态 |
|-------|---------|------|
| cdc-lag-monitor | 增量复制延迟监控 | 已发布 |
| top-error-monitor | 报错监控（含收敛、归一化、归因、修复指引） | 开发中 |
| {database}-log-monitor | 数据库 CDC 相关配置监控 | 开发中 |

#### 3.7.2 归因分析

- **上下文分析**：利用错误栈匹配知识库，自动产生修复指引
- **统计学分析**：性能诊断和瓶颈分析
- **流程改进**：给出业务数据库参数调优建议

#### 3.7.3 通知策略

支持通过前台界面配置监控告警规则，灵活设置通知策略。

### 3.8 用户管理

- 管理员可添加和修改用户
- 支持账号密码及第三方账号登录
- 许可证在线更新

---

## 4. 技术架构

### 4.1 流批融合架构

RTSync 采用**流批融合架构**，将全量迁移（Batch）和增量复制（Stream）统一在同一引擎中，实现：

- **流式复制表之间解耦**：各表的增量复制互不干扰
- **全增量融合技术**：全量迁移完成后无缝切换至增量模式，无需人工干预
- **性能显著提升**：流式复制平均约 **60%** 的性能提升

### 4.2 连接器解耦设计

RTSync 的所有连接器之间互相解耦，采用插件化架构。创建作业时，用户可以根据各连接器的读写能力支持状态进行任意排列组合。

> 例如：由于支持了 Oracle 的读取，因此您可以将 Oracle 中的数据复制到任何支持写入的数据库中。

### 4.3 核心技术栈

| 层次 | 技术选型 |
|-----|---------|
| **前端控制台** | React SPA 架构 |
| **文档系统** | Docusaurus v3.6.3 |
| **容器化** | Docker 支持，Playground 模式 |
| **性能测试** | 内置 TPC-H / TPC-DS 数据生成，Netperf 网络基准 |

### 4.4 数据同步流程

```
源数据库 → [连接器-读取端] → [同步引擎] → [连接器-写入端] → 目标数据库
    ↓                          ↓                           ↓
 Binlog/Redo Log          流批融合处理              JDBC/Load Data
 CDC 日志解析            事务排序与合并              批量写入优化
```

### 4.5 End-to-End 性能瓶颈分析

RTSync 提供科学的性能分析方法论。End-to-End 的数据复制性能受限于三个核心因素：

1. **读端数据库的读取能力**
2. **写端数据库的写入能力**
3. **中间网络带宽**

由于木桶效应，实际运行性能取决于上述环节中性能最差的模块。RTSync 内置 **Dummy 连接器**和 **Netperf 集成**，帮助用户快速定量地完成基准测试，精确定位性能瓶颈。

---

## 5. 支持的数据库与连接器

RTSync 当前支持 **23 种** 数据库连接器，覆盖主流商业数据库、开源数据库、国产数据库及消息中间件。

### 5.1 连接器能力全景表

| 数据库 | 内核版本 | 全量读取 | 增量读取(CDC) | 写入 | 数据比对 |
|-------|---------|---------|-------------|-----|---------|
| **Oracle** | 10.2 - 23.0 | ✓ | ✓ | ✓ | ✓ |
| **MySQL** | 5.7 - 9.1 | ✓ | ✓ | ✓ | ✓ |
| **SQL Server** | 10 - 15 | ✓ | ✓ | ✓ | ✓ |
| **PostgreSQL** | 12.0 - 17.5 | ✓ | ✓ | ✓ | ✓ |
| **OceanBase** | 4.x | ✓ | ✓ | ✓ | ✓ |
| **达梦 DM8** | 8.x | ✓ | ✓ | ✓ | ✓ |
| **KingbaseES** | 8.x | ✓ | ✓ | ✓ | ✓ |
| **GaussDB** | - | ✓ | ✓ | ✓ | ✓ |
| **OpenGauss** | - | ✓ | ✓ | ✓ | ✓ |
| **VastBase** | - | ✓ | ✓ | ✓ | ✓ |
| **GBase 8a** | 9.5.x | ✓ | - | ✓ | ✓ |
| **GBase 8s** | 8.8.x | ✓ | ✓ | ✓ | ✓ |
| **GBase 8s (PG 兼容版)** | - | ✓ | ✓ | ✓ | ✓ |
| **GoldenDB** | 6.1.x | ✓ | ✓ | - | ✓ |
| **MatrixDB** | 12.0 - 17.5 | ✓ | ✓ | ✓ | ✓ |
| **Apache Doris** | 2.0 - 3.0 | - | - | ✓ | ✓ |
| **Apache Kafka** | 0.11 - 3.7 | ✓ | ✓ | ✓ | - |
| **Apache Kafka v0.9** | 0.9.0 | ✓ | ✓ | ✓ | - |
| **Hive 3** | 3.x | - | - | ✓ | - |
| **Informix** | 11.50 | ✓ | ✓ | ✓ | - |
| **TDSQL-MySQL** | 5.7 - 9.1 | ✓ | ✓ | ✓ | ✓ |
| **GBase 8s v2.2.0** | 8.8.x | ✓ | ✓ | ✓ | - |
| **Dummy (数据生成器)** | - | ✓ | ✓ | ✓ | - |

### 5.2 国产数据库深度适配

RTSync 对国产数据库提供了深度的适配和优化：

- **达梦 DM8**：支持全量读写 + CDC 日志订阅 + 备库支持，持续优化 CDC 性能
- **人大金仓 KingbaseES**：支持全量读写 + CDC 日志订阅，优化 CDC 消费性能
- **南大通用 GBase 8a/8s**：支持 Load Data 高性能写入、两阶段提交、HttpServer 方式 Load 数据
- **华为 GaussDB**：支持分布式架构下的增量复制 + 日志订阅
- **OceanBase**：支持 Oracle 模式 + 日志订阅
- **GoldenDB**：支持全量读 + 日志订阅
- **VastBase**：支持日志订阅
- **OpenGauss**：支持日志订阅

### 5.3 消息中间件集成

- **Apache Kafka**：支持 0.9.0 至 3.7.0 全版本覆盖，支持读写双向、Schema 预测、点位回调、Rebalance 优化
- **Apache Doris**：支持 Stream Load 高性能写入
- **Hive 3**：支持数据写入，用于构建数据湖/数据仓库

---

## 6. 典型应用场景

### 6.1 数据库国产化迁移

**场景描述：** 将 Oracle/SQL Server 等商业数据库迁移至达梦、人大金仓、GBase 等国产数据库。

**RTSync 方案：**
1. 创建源库（Oracle）和目标库（达梦）的连接
2. 建立作业，选择需要迁移的表
3. 执行全量迁移，将存量数据复制到目标库
4. 开启 CDC 实时复制，同步增量数据
5. 使用数据比对功能验证迁移完整性
6. 确认数据一致后，切换业务系统至新库

### 6.2 实时数据仓库构建

**场景描述：** 将在线业务数据库的变更数据实时同步到分析型数据库（如 Apache Doris、Hive）。

**RTSync 方案：**
1. 配置 MySQL/Oracle 作为源端
2. 配置 Apache Doris 或 Hive 作为写入端
3. 开启 CDC 实时复制，持续将业务变更同步至数据仓库
4. 实现 T+0 的实时报表和分析能力

### 6.3 金融交易系统实时备份

**场景描述：** 金融系统需要实时备份，确保数据零丢失和快速灾备切换。

**RTSync 方案：**
1. 配置生产库为源端，灾备库为目标端
2. 使用 CDC 实时复制，保持毫秒级同步
3. 利用断点续传能力，保证网络中断后自动恢复
4. 定期执行数据比对，确保灾备数据完整

### 6.4 微服务数据同步

**场景描述：** 微服务架构下，各服务使用独立数据库，需要跨服务的数据一致性。

**RTSync 方案：**
1. 利用 Kafka 作为中间层，实现事件驱动的数据同步
2. 源端数据库变更通过 CDC 写入 Kafka
3. 目标服务从 Kafka 消费数据写入各自数据库
4. Schema 预测功能自动适配数据结构变化

### 6.5 测试环境数据初始化

**场景描述：** 快速将生产数据复制到测试/开发环境。

**RTSync 方案：**
1. 使用全量迁移功能一键复制整库数据
2. 支持跨异构数据库的数据复制（如从 Oracle 复制到 MySQL）
3. Dummy 连接器可生成 TPC-H/TPC-DS 标准测试数据集

---

## 7. 性能指标与基准测试

### 7.1 性能架构

RTSync 的 End-to-End 数据复制性能由三个核心环节决定：

```
[源端读取能力] ←→ [网络带宽] ←→ [目标端写入能力]
```

由于木桶效应，实际性能取决于上述环节中性能最差的模块。

### 7.2 内置基准测试工具

#### Dummy 连接器

RTSync 内置了 Dummy 连接器，用于科学的性能基准测试：

- **作为读端**：内置 TPC-H、TPC-DS 等数据生成算法，可快速构建测试数据集
- **作为写端**：接收数据写入请求并直接丢弃，用于纯粹测量源端读取性能

**测试方法：**
- **写入能力测试**：创建 Dummy → 目标数据库 的作业，测量目标库的写入极限
- **读取能力测试**：创建 源数据库 → Dummy 的作业，测量源库的读取极限

#### Netperf 网络基准

集成 Netperf 工具，支持：
- **带宽吞吐测试**（TCP_STREAM 模式）
- **延迟测试**（TCP_RR 模式）

### 7.3 性能优化亮点

| 优化项 | 描述 |
|-------|------|
| 流批融合架构 | 流式复制平均约 60% 性能提升 |
| GBase 8a Load Data | 增量写 Load 性能优化，两阶段提交 |
| HttpServer Load | 支持通过 HttpServer 方式 Load 数据 |
| Schema 加载优化 | CDC 初始化 Schema 加载性能提升 |
| 内存优化 | Kafka 连接器内存使用优化 |
| 并发控制 | 读取/写入端最大并发数可调整 |
| 任务调度优化 | 降低内存使用，提升稳定性 |

---

## 8. 运维与监控体系

### 8.1 监控架构

RTSync 提供三层监控体系：

1. **监视器层**：实时检测各类异常，产生监控事件
2. **归因分析层**：通过上下文分析、统计学分析定位根因，并给出修复指引
3. **通知策略层**：根据配置的规则向相关人员发送告警通知

### 8.2 监控指标

| 指标类型 | 说明 |
|---------|------|
| CDC 延迟 | 实时监控增量复制的延迟时间 |
| 同步速率 | 数据同步的吞吐量指标 |
| 错误统计 | 报错收敛、归一化处理，避免告警风暴 |
| 数据库日志配置 | 检查 CDC 相关配置是否正确（表级别日志等级、日志存留时间、磁盘空间） |

### 8.3 指标与生态集成

支持与外部监控系统集成，导出指标数据，融入企业已有的运维监控生态。

### 8.4 日志管理

- 支持作业级别日志记录
- 日志筛选可按对象名合并
- 展示最新一条日志记录，快速定位问题
- SQL Server 连接器新增日志监控和 CDC 开启诊断

---

## 9. 安全与合规

### 9.1 访问控制

- 用户管理系统，管理员可创建和管理用户账号
- 支持账号密码认证及第三方账号登录
- 基于角色的权限控制

### 9.2 许可证管理

- 许可证在线更新机制，无需停机
- 灵活的许可证策略，按需扩展

### 9.3 数据安全

- 数据传输过程中保持事务原子性和顺序
- 断点续传机制确保数据零丢失
- 严格模式下异常数据立即报错，确保数据质量

---

## 10. 部署架构

### 10.1 部署方式

| 部署方式 | 说明 |
|---------|------|
| **Playground 模式** | 基于 Docker 快速体验，适合评估测试 |
| **标准部署** | 生产环境部署，支持独立安装 |
| **高可用部署** | 支持故障转移 + 负载均衡的高可用架构 |

### 10.2 高可用特性

- **故障转移（Failover）**：CDC Failover 策略持续优化，自动切换至健康节点
- **负载均衡**：数据库高可用支持，多节点分担负载
- **断点续传**：任何原因导致同步中断（网络断开、服务器断电等），恢复后自动从记录的位点继续

### 10.3 环境要求

- 可访问公网的环境（Playground 模式需从 Docker Hub 拉取镜像）
- 支持现代浏览器访问控制台（最低兼容 Chrome 88）

---

## 11. 版本演进路线

RTSync 保持高频迭代，从 v2.1 到 v2.14 共发布 14 个版本，展现了强劲的产品演进能力。

### 11.1 关键里程碑

| 版本 | 日期 | 核心特性 |
|------|------|---------|
| **v2.1** | 2024.11 | 流批融合架构上线，流式复制性能提升 60%；监控报警体系；OceanBase/达梦连接器 |
| **v2.2** | 2024.12 | 用户管理功能；许可证在线更新；数据比对扩充浮点/定点数 |
| **v2.3** | 2025.01 | 脏数据兼容模式；KingbaseES 连接器；数据比对交互优化 |
| **v2.4** | 2025.01 | 达梦 CDC 日志订阅；GoldenDB 连接器 |
| **v2.5** | 2025.03 | SQL Server 连接器；MySQL 视图/触发器/存储过程迁移 |
| **v2.6** | 2025.06 | Apache Kafka / Apache Doris 连接器 |
| **v2.7** | 2025.07 | SQL Server / KingbaseES CDC 日志订阅 |
| **v2.8** | 2025.10 | GBase 8a Load Data 写入；10 个连接器 CDC 日志订阅 |
| **v2.9** | 2025.11 | Kafka Schema 预测；GBase 8a 两阶段提交性能优化 |
| **v2.10** | 2026.01 | 前台监控告警配置；GaussDB 分布式增量复制；CDC Failover 优化 |
| **v2.11** | 2026.02 | 自动数据比对；批量编辑表；CDC 起始点位设置 |
| **v2.12** | 2026.02 | 高可用部署；多连接器 Bug 修复与性能优化 |
| **v2.13** | 2026.03 | 大小写策略迁移至作业级别；GBase 8a Load Data 增量写入 |
| **v2.14** | 2026.03 | 日志筛选优化；GBase 8a 增量写 Load 性能优化；Hive 3 连接器；任务调度优化 |

### 11.2 持续演进方向

- 更多连接器的 CDC 能力完善
- 监控报错的归因分析与自动修复指引
- 数据库日志配置的自动检测与诊断
- 比对过滤条件支持，降低增量数据对比对结果的影响

---

## 12. 总结

RTSync 是一款定位清晰、功能完备的企业级异构数据库实时同步平台。其核心价值在于：

1. **广泛的异构兼容性**：23+ 种连接器覆盖主流商业、开源、国产数据库和消息中间件，连接器之间完全解耦，支持任意组合
2. **完整的同步链路**：提供全量迁移 → 增量实时同步 → 数据一致性比对的端到端解决方案
3. **深度国产适配**：对达梦、人大金仓、GBase、GaussDB、OceanBase 等国产数据库提供深度优化和持续迭代
4. **企业级可靠性**：高可用部署、断点续传、故障转移、CDC Failover 等机制确保生产环境可靠运行
5. **智能化运维**：监控事件全生命周期跟踪、归因分析、修复指引，降低运维成本
6. **高性能引擎**：流批融合架构带来约 60% 的性能提升，持续的写入优化（Load Data、两阶段提交）满足高吞吐场景

RTSync 凭借其在异构数据库同步领域的专业能力和对国产数据库生态的深度适配，已成为企业数据库迁移和实时同步场景中值得信赖的基础设施产品。

---

*本白皮书基于 RTSync v2.14 版本编写，产品功能持续迭代更新中。*

*版权所有 &copy; 2026 1stBlue. All Rights Reserved.*
