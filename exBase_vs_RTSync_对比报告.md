# exBase vs RTSync(Bluepipe) 产品对比分析报告

---

**文档版本**：1.0
**编制日期**：2026年4月
**对比版本**：exBase V4.9.1 / RTSync(Bluepipe) v2.14

---

## 目录

1. [报告概要](#1-报告概要)
2. [厂商背景对比](#2-厂商背景对比)
3. [产品定位与核心理念](#3-产品定位与核心理念)
4. [系统架构对比](#4-系统架构对比)
5. [核心功能全景对比](#5-核心功能全景对比)
6. [数据库连接器支持对比](#6-数据库连接器支持对比)
7. [迁移评估能力对比](#7-迁移评估能力对比)
8. [全量迁移能力对比](#8-全量迁移能力对比)
9. [增量同步能力对比](#9-增量同步能力对比)
10. [数据校验与比对能力对比](#10-数据校验与比对能力对比)
11. [DDL 结构迁移与 SQL 转换对比](#11-ddl-结构迁移与-sql-转换对比)
12. [冲突处理与容错机制对比](#12-冲突处理与容错机制对比)
13. [监控与运维对比](#13-监控与运维对比)
14. [部署架构与高可用对比](#14-部署架构与高可用对比)
15. [性能特性对比](#15-性能特性对比)
16. [适用场景对比](#16-适用场景对比)
17. [产品成熟度与生态对比](#17-产品成熟度与生态对比)
18. [选型建议](#18-选型建议)
19. [总结](#19-总结)

---

## 1. 报告概要

本报告对**海量数据 exBase**（一键式异构数据库迁移系统）与**1stBlue RTSync/Bluepipe**（异构数据库实时同步平台）进行全面、深入的功能对比分析，旨在帮助企业在数据库国产化替代和数据同步场景中做出合理的技术选型。

**核心结论概览**：

| 维度 | exBase | RTSync(Bluepipe) |
|------|--------|------------------|
| **核心定位** | 一次性迁移割接 | 持续性实时同步 |
| **最强项** | 迁移评估 + SQL/DDL 自动转换 | CDC 实时复制 + 数据比对 |
| **目标场景** | 去O/信创数据库替代 | 异构数据库持续同步 |
| **迁移完整度** | 含应用层评估与改造 | 仅数据层同步 |
| **长期运行** | 迁移完成后可退出 | 设计为长期运行 |

---

## 2. 厂商背景对比

| 维度 | exBase | RTSync(Bluepipe) |
|------|--------|------------------|
| **开发商** | 北京海量数据技术股份有限公司 | 1stBlue |
| **股票代码** | 603138.SH（A股主板） | 非上市 |
| **成立时间** | 2007年 | - |
| **主营业务** | 数据库产品研发、销售和服务 | 数据同步中间件 |
| **核心产品线** | Vastbase 数据库系列、Vastcube 一体机、exBase 迁移工具 | Bluepipe/RTSync 数据同步平台 |
| **行业定位** | 国内首家以数据库为主营业务的主板上市企业 | 数据集成领域专业厂商 |
| **行业覆盖** | 政务、制造、金融、通信、能源、交通 | 通用行业 |
| **产品关系** | exBase 为 Vastbase 数据库生态的配套迁移工具 | 独立的数据同步中间件产品 |
| **商业模式** | 商业授权（无免费版） | 商业授权 |
| **官方文档** | docs.vastdata.com.cn | www.1stblue.com |

**分析**：exBase 背靠上市公司海量数据，拥有完整的数据库产品生态（Vastbase + exBase + Vastcube），在政企市场具有品牌信任度优势。RTSync/Bluepipe 作为独立的数据同步产品，强调通用性和中立性，不绑定特定数据库厂商。

---

## 3. 产品定位与核心理念

### 3.1 exBase —— "一键式异构数据库迁移系统"

exBase 的核心理念是将**数据库迁移全生命周期**封装为一键式流程：

```
迁移评估 → 应用评估 → 自动采集 → 结构迁移 → 数据迁移 → 数据校验
```

- 定位为**项目制的迁移工具**，服务于有明确起止时间的数据库替代项目
- 强调**迁移前的可行性评估和工作量预估**
- 核心价值是帮助用户从 Oracle/SQL Server 等商业数据库迁移至 Vastbase/openGauss 等国产数据库
- 包含**应用层面的 SQL 兼容性分析和自动改造**能力

### 3.2 RTSync(Bluepipe) —— "异构数据库实时同步平台"

RTSync 的核心理念是**数据因自由流动而价值无限**：

```
连接管理 → 作业配置 → 全量迁移 → 增量实时复制 → 持续数据比对
```

- 定位为**长期运行的数据同步基础设施**
- 强调**实时性**（毫秒级 CDC 延迟）和**持续数据一致性**
- 核心价值是任意异构数据库之间的持续数据同步
- 采用 **Exactly Once** 一致性保证语义
- 提供 API/SDK 与现有系统无缝集成

### 3.3 定位差异总结

```
                    ┌─────────────────────────────────────────────────┐
                    │              数据库生命周期                       │
                    │                                                 │
  ┌───────────┐     │   评估   采集   迁移   校验   同步   比对   监控  │
  │  exBase   │────▶│   ████   ████   ████   ████   ███              │
  └───────────┘     │   ◄──── 迁移项目周期 ────►                      │
                    │                                                 │
  ┌───────────┐     │                        ████   ████   ████  ████ │
  │  RTSync   │────▶│                  ████   ████   ████   ████  ████│
  └───────────┘     │                  ◄───── 持续运行 ──────────────► │
                    └─────────────────────────────────────────────────┘
```

---

## 4. 系统架构对比

### 4.1 架构总览

| 维度 | exBase | RTSync(Bluepipe) |
|------|--------|------------------|
| **整体架构** | B/S 架构 | B/S 架构（React SPA） |
| **访问协议** | HTTP / HTTPS | HTTP / HTTPS |
| **数据库访问引擎** | JDBC（TCP/IP） | 连接器插件化 |
| **系统元数据存储** | 内置 Vastbase 数据库 | 未公开 |
| **消息中间件** | 内嵌 Kafka（增量队列缓存） | 未使用外部 MQ |
| **文档系统** | Vastdata 知识中心 | Docusaurus v3.6.3 |
| **容器化支持** | - | Docker Playground 模式 |
| **API/SDK** | Web UI + API 接口 | 声明式 API + SDK |

### 4.2 exBase 架构细节

```
┌─────────────────────────────────────────────────────────┐
│                      WEB UI (B/S)                       │
├─────────────────────────────────────────────────────────┤
│                    任务调度与管理层                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ 评估引擎  │  │ 转换引擎  │  │ 迁移引擎  │  │校验引擎  │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
├─────────────────────────────────────────────────────────┤
│  ┌──────────────┐    ┌───────────────────────────────┐  │
│  │ 内嵌 Kafka    │    │  迁移规则模板库                 │  │
│  │（增量缓存队列）│    │  (ora2vastbase_v2.2.10 等)     │  │
│  └──────────────┘    └───────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│  ┌────────────────┐           ┌────────────────┐        │
│  │ 内置 Vastbase DB│           │   JDBC 引擎    │        │
│  │ (系统元数据存储) │           │(源库/目标库访问) │        │
│  └────────────────┘           └────────────────┘        │
└─────────────────────────────────────────────────────────┘
         │                              │
    ┌────▼────┐                    ┌────▼────┐
    │  源数据库 │                    │ 目标数据库 │
    │Oracle/   │                    │Vastbase/ │
    │MySQL/DB2 │                    │openGauss │
    └─────────┘                    └─────────┘
```

### 4.3 RTSync(Bluepipe) 架构细节

```
┌─────────────────────────────────────────────────────────┐
│                  React SPA 控制台                        │
├─────────────────────────────────────────────────────────┤
│                   同步引擎（流批融合）                     │
│  ┌──────────────────┐    ┌──────────────────┐           │
│  │  连接器-读取端     │    │  连接器-写入端     │           │
│  │  (Binlog/Redo/   │────│  (JDBC/Load/     │           │
│  │   WAL 日志解析)   │    │   Stream Load)   │           │
│  └──────────────────┘    └──────────────────┘           │
│  ┌──────────────┐  ┌──────────┐  ┌────────────────┐    │
│  │ 数据比对引擎   │  │ 监控体系  │  │ 任务调度与管理   │    │
│  │(自动/MD5全文) │  │(事件跟踪) │  │(故障转移/均衡)  │    │
│  └──────────────┘  └──────────┘  └────────────────┘    │
├─────────────────────────────────────────────────────────┤
│                  23+ 连接器插件库                         │
│  Oracle│MySQL│PG│SQLServer│达梦│金仓│GBase│Kafka│...   │
└─────────────────────────────────────────────────────────┘
         │                              │
    ┌────▼────┐                    ┌────▼────┐
    │  源数据库 │◄──── 双向 ────►    │ 目标数据库 │
    └─────────┘                    └─────────┘
```

### 4.4 架构差异分析

| 维度 | exBase | RTSync |
|------|--------|--------|
| **连接器设计** | 统一 JDBC 引擎 | 独立插件化连接器（互相解耦） |
| **规则引擎** | 内置迁移规则模板库，支持自定义 | 无语法转换规则 |
| **消息队列依赖** | 内嵌 Kafka（增量缓存） | 无外部 MQ 依赖 |
| **引擎特性** | 评估+转换+迁移+校验 多引擎 | 流批融合统一引擎 |
| **可集成性** | Web UI + API | API + SDK + 声明式接口 |

---

## 5. 核心功能全景对比

| 功能模块 | exBase | RTSync | 对比说明 |
|---------|--------|--------|---------|
| **数据库对象兼容性评估** | ★★★★★ | - | exBase 独有，RTSync 无此能力 |
| **应用 SQL 兼容性评估** | ★★★★★ | - | exBase 独有，支持扫描数据库和 Mapper 文件 |
| **工作量/耗时预估** | ★★★★★ | - | exBase 独有，辅助项目规划 |
| **DDL 结构迁移** | ★★★★★ | ★★★ | exBase 含智能转换；RTSync 支持自动建表 |
| **PL/SQL 代码转换** | ★★★★★ | - | exBase 独有，自动改写存储过程/函数 |
| **全量数据迁移** | ★★★★ | ★★★★★ | 两者均支持，RTSync 更强调性能优化 |
| **增量实时同步(CDC)** | ★★★★ | ★★★★★ | RTSync 更成熟，23种连接器的 CDC 支持 |
| **正向增量同步** | ★★★★ | ★★★★★ | 两者均支持 |
| **反向增量同步** | ★★★★ | - | exBase 独有，支持回切场景 |
| **数据校验/比对** | ★★★★ | ★★★★★ | RTSync 有自动比对+MD5 全文比对双模式 |
| **断点续传** | ★★★★ | ★★★★★ | 两者均支持 |
| **冲突处理** | ★★★★★ | ★★★ | exBase 更完善（自动/手动跳过/覆盖） |
| **迁移规则管理** | ★★★★★ | - | exBase 独有，内置模板+自定义 |
| **监控告警体系** | ★★ | ★★★★ | RTSync 更完善的三层监控架构 |
| **高可用部署** | ★★ | ★★★★ | RTSync 支持故障转移+负载均衡 |
| **用户管理** | ★★ | ★★★★ | RTSync 有完整的用户管理系统 |
| **API/SDK 集成** | ★★★ | ★★★★ | RTSync 提供声明式 API 和 SDK |
| **性能基准测试工具** | - | ★★★★ | RTSync 内置 Dummy 连接器+Netperf |
| **Playground 快速体验** | - | ★★★★★ | RTSync 支持 Docker Playground |

---

## 6. 数据库连接器支持对比

### 6.1 源端数据库

| 数据库 | exBase（源端） | RTSync（源端） |
|-------|--------------|--------------|
| **Oracle** | ✓ (含 PDB 模式) | ✓ (10.2-23.0, 含 PDB) |
| **MySQL** | ✓ | ✓ (5.7-9.1) |
| **SQL Server** | ✓ | ✓ (10-15) |
| **DB2** | ✓ | - |
| **PostgreSQL** | ✓ | ✓ (12.0-17.5) |
| **达梦 DM8** | ✓ | ✓ (8.x) |
| **Informix** | ✓ | ✓ (11.50) |
| **SQLite** | ✓ | - |
| **KingbaseES** | ✓ | ✓ (8.x) |
| **GBase 8s** | ✓ | ✓ (8.8.x) |
| **OceanBase** | - | ✓ (4.x) |
| **GBase 8a** | - | ✓ (9.5.x，仅全量) |
| **GoldenDB** | - | ✓ (6.1.x) |
| **GaussDB** | - | ✓ |
| **OpenGauss** | - | ✓ |
| **VastBase** | - | ✓ |
| **Apache Kafka** | - | ✓ (0.11-3.7) |
| **MatrixDB** | - | ✓ (12.0-17.5) |
| **TDSQL-MySQL** | - | ✓ (5.7-9.1) |
| **Dummy(数据生成器)** | - | ✓ |

### 6.2 目标端数据库

| 数据库 | exBase（目标端） | RTSync（目标端） |
|-------|----------------|----------------|
| **Vastbase G100** | ✓ (首要目标) | ✓ |
| **Vastbase E100** | ✓ | - |
| **openGauss** | ✓ | ✓ |
| **GaussDB** | ✓ | ✓ |
| **PostgreSQL** | ✓ | ✓ |
| **MySQL** | ✓ | ✓ |
| **Kafka** | ✓ | ✓ |
| **Oracle** | - | ✓ |
| **SQL Server** | - | ✓ |
| **达梦 DM8** | - | ✓ |
| **KingbaseES** | - | ✓ |
| **GBase 8a** | - | ✓ |
| **GBase 8s** | - | ✓ |
| **OceanBase** | - | ✓ |
| **Apache Doris** | - | ✓ (2.0-3.0) |
| **Hive 3** | - | ✓ (3.x) |
| **MatrixDB** | - | ✓ |
| **TDSQL-MySQL** | - | ✓ |
| **GoldenDB** | - | - |

### 6.3 连接器对比分析

| 维度 | exBase | RTSync |
|------|--------|--------|
| **源端连接器数量** | ~10种 | 23种 |
| **目标端连接器数量** | ~7种 | 20+种 |
| **DB2 支持** | ✓ | - |
| **SQLite 支持** | ✓ | - |
| **写入方向** | 主要单向（源→国产库） | 任意方向组合 |
| **连接器耦合度** | 统一 JDBC 引擎 | 互相解耦，插件化 |
| **连接器迭代速度** | 中等 | 快（14个版本持续新增） |

**分析**：RTSync 在连接器数量上有明显优势（23 vs ~10），且支持任意方向的读写组合。exBase 的连接器更聚焦于"外库→国产库"的单向迁移，但独有 DB2 和 SQLite 的源端支持。

---

## 7. 迁移评估能力对比

这是两个产品差异最显著的维度。

### 7.1 对象兼容性评估

| 能力 | exBase | RTSync |
|------|--------|--------|
| **DDL 自动扫描采集** | ✓ | - |
| **兼容性三级分类** | ✓（兼容/可转换/不兼容） | - |
| **可视化分析报告** | ✓ | - |
| **数据量统计** | ✓ | - |
| **迁移耗时评估** | ✓ | - |
| **改造工作量评估** | ✓ | - |
| **不兼容对象改造建议** | ✓ | - |

### 7.2 应用 SQL 兼容性评估

| 能力 | exBase | RTSync |
|------|--------|--------|
| **扫描数据库会话 SQL** | ✓（JDBC 持续采集） | - |
| **扫描 MyBatis Mapper 文件** | ✓ | - |
| **SQL 脱敏去重** | ✓ | - |
| **SQL 兼容性分类** | ✓（兼容/可转换/需重写） | - |
| **SQL 改造建议** | ✓ | - |
| **应用兼容性报告** | ✓ | - |

### 7.3 评估能力总结

exBase 在迁移评估维度具有**绝对优势**，这是其产品的核心差异化竞争力。RTSync 完全不具备迁移前评估能力——它假设用户已经做好了迁移决策和目标库选择，直接进入数据同步环节。

exBase 的评估能力对企业的实际价值：
- **项目可行性论证**：量化评估迁移难度，辅助决策
- **项目规划**：精确预估工作量和耗时，指导资源分配
- **风险控制**：提前识别不兼容项，规避迁移后才发现的问题
- **成本控制**：量化人工改造成本，辅助预算编制

---

## 8. 全量迁移能力对比

| 能力 | exBase | RTSync |
|------|--------|--------|
| **单表全量迁移** | ✓ | ✓ |
| **多表批量迁移** | ✓ | ✓（勾选多表） |
| **整库全量迁移** | ✓（一键） | ✓（设置页签触发） |
| **WHERE 条件筛选** | ✓ | - |
| **字符集自动转换** | ✓ | - |
| **大小写策略** | ✓ | ✓（保持/转小写/转大写） |
| **对象与数据分离迁移** | ✓（DDL 和数据可独立执行） | -（统一处理） |
| **DDL 手动修改后重执行** | ✓ | - |
| **迁移进度可视化** | ✓ | ✓ |
| **迁移速率展示** | ✓ | ✓ |
| **全量同步自动清理目标表** | - | ✓（可配置） |
| **数据库快照机制** | - | ✓（Flashback/FLUSH TABLES） |
| **视图/触发器/存储过程迁移** | ✓（自动转换） | ✓（仅 MySQL 之间） |

**分析**：exBase 在全量迁移中更注重**异构转换**能力（字符集、DDL 修改、对象与数据分离）；RTSync 更注重**执行效率和灵活性**（快照机制、自动清理、多种触发方式）。

---

## 9. 增量同步能力对比

| 能力 | exBase | RTSync |
|------|--------|--------|
| **CDC 技术** | ✓（日志解析） | ✓（日志解析） |
| **日志类型支持** | 本地数据库日志 | Binlog/Redo Log/WAL 等 |
| **同步延迟** | 实时 | 毫秒级 |
| **正向增量同步** | ✓ | ✓ |
| **反向增量同步** | ✓ | - |
| **CDC 位点管理** | ✓ | ✓（可视化展示） |
| **CDC 起始点位设置** | - | ✓ |
| **断点续传** | ✓（自动） | ✓（自动，含异常恢复） |
| **Kafka 缓存队列** | ✓（内嵌 Kafka） | -（无外部 MQ 依赖） |
| **增量事务保序** | ✓ | ✓（事务原子性和顺序） |
| **全增量融合** | - | ✓（流批融合架构） |
| **CDC Failover** | - | ✓（策略持续优化） |
| **CDC 连接器数量** | 有限 | 大量（Oracle/MySQL/PG/SQLServer/达梦/金仓/GBase/Informix/OceanBase/GaussDB/OpenGauss/VastBase/Kafka/TDSQL等） |
| **增量复制延迟监控** | - | ✓（cdc-lag-monitor） |
| **支持的 CDC 数据库数** | ~5种 | 18+种 |

### 9.1 关键差异：反向增量同步

exBase 支持**反向增量同步**，这是其在迁移场景中的重要特性：

```
正向同步：Oracle ──────► Vastbase    （迁移方向）
反向同步：Oracle ◄────── Vastbase    （回切保障）
```

在数据库切换后，如果新库出现问题需要回切旧库，反向增量同步可以确保在回切过程中数据不丢失。这对于关键业务系统的数据库替代项目尤为重要。

RTSync 不支持反向同步，但其任意方向的连接器组合在一定程度上可以通过创建反向作业来实现类似效果。

### 9.2 关键差异：流批融合

RTSync 的流批融合架构是其核心技术优势：
- 全量迁移完成后**无缝切换**至增量模式
- 无需人工干预
- 流式复制性能提升约 **60%**
- 各表的增量复制互不干扰

exBase 的全量和增量是独立的步骤，需要分别配置和触发。

---

## 10. 数据校验与比对能力对比

| 能力 | exBase | RTSync |
|------|--------|--------|
| **迁移结果校验** | ✓ | - |
| **对象存在性校验** | ✓ | - |
| **表数据一致性校验** | ✓ | ✓ |
| **自动比对（统计信息）** | - | ✓（COUNT/SUM 聚合查询） |
| **全文比对（MD5 哈希）** | - | ✓（逐行指纹对比） |
| **定时自动比对** | - | ✓ |
| **比对结果可视化** | ✓（校验报告） | ✓（差异详情展示） |
| **浮点/定点数比对** | - | ✓ |
| **持续比对能力** | 一次性 | 持续/定时 |
| **比对并发控制** | - | ✓（最大4并发，单表10min超时） |
| **比对对源库影响** | - | 可控（轻量级聚合查询） |

### 10.1 比对方式对比

**exBase 数据校验**：
- 定位为迁移完成后的**一次性验证**
- 验证对象是否成功创建 + 数据是否一致
- 生成校验结果报告

**RTSync 数据比对**（双模式）：

| 比对方式 | 触发方式 | 原理 | 准确性 | 效率 | 适用场景 |
|---------|---------|------|--------|------|---------|
| 自动比对 | 自动/定时 | COUNT/SUM 聚合 | 高 | 高 | 日常巡检 |
| 全文比对 | 人工触发 | MD5 哈希逐行 | 100% | 中 | 精细验证 |

**分析**：RTSync 的数据比对能力明显更强，支持持续运行的双模式比对，适合长期监控数据一致性。exBase 的校验更偏向迁移后的一次性确认。

---

## 11. DDL 结构迁移与 SQL 转换对比

这是 exBase 的**核心技术壁垒**。

| 能力 | exBase | RTSync |
|------|--------|--------|
| **表结构自动转换** | ✓（异构语法转换） | ✓（自动建表） |
| **索引迁移** | ✓（自动转换） | ✓（有限支持） |
| **序列迁移** | ✓（自动转换） | - |
| **视图迁移** | ✓（自动转换） | ✓（仅 MySQL 间） |
| **存储过程迁移** | ✓（PL/SQL 自动改写） | ✓（仅 MySQL 间） |
| **触发器迁移** | ✓（自动转换） | ✓（仅 MySQL 间） |
| **自定义函数迁移** | ✓（自动转换） | - |
| **同义词迁移** | ✓ | - |
| **物化视图迁移** | ✓ | - |
| **外部过程迁移** | ✓ | - |
| **迁移规则模板** | ✓（如 ora2vastbase_v2.2.10） | - |
| **自定义迁移规则** | ✓ | - |
| **DDL 结构变更同步(Alter Table)** | - | ✓（自动跟踪并应用） |

### 11.1 exBase 的 PL/SQL 转换能力

exBase 能自动将 Oracle 的 PL/SQL 代码改写为目标库兼容语法，覆盖：
- 存储过程
- 自定义函数
- 外部过程
- 包（Package）

对于依赖存储过程和函数进行复杂数据逻辑处理的应用，这一能力可免去 **50% 或以上的人工改写工作量**。

### 11.2 RTSync 的自动结构跟踪

RTSync 虽然不进行语法转换，但支持**自动跟踪数据结构变化并应用到写入端**，抹平异构数据库之间的差异。这一能力在长期运行场景中更有价值——源端表结构变更时无需手动干预。

---

## 12. 冲突处理与容错机制对比

| 能力 | exBase | RTSync |
|------|--------|--------|
| **INSERT 主键冲突** | ✓（跳过/覆盖） | ✓ |
| **INSERT 唯一键冲突** | ✓（跳过/覆盖） | ✓ |
| **UPDATE 数据冲突** | ✓（跳过/更新） | ✓ |
| **UPDATE 找不到行** | ✓（跳过/记录） | ✓ |
| **DELETE 找不到行** | ✓（跳过/记录） | ✓ |
| **自动冲突处理** | ✓（预配置策略） | ✓ |
| **手动冲突处理** | ✓（中止后手动干预） | - |
| **异常信息查询** | ✓（Kafka 上报） | ✓（日志系统） |
| **脏数据处理策略** | - | ✓（严格模式/兼容模式） |
| **Alter Table 策略** | - | ✓（表级别配置） |

### 12.1 exBase 冲突处理流程

```
数据冲突 → 异常定位 → Kafka上报 → 策略判断
                                     ├── 自动策略 → 直接处理（跳过/覆盖）→ 继续同步
                                     └── 未配置   → 任务中止 → 人工配置 → 重试
```

### 12.2 RTSync 脏数据处理

```
异常数据 → 模式判断
             ├── 严格模式（Strict Mode）  → 立即报错中止
             └── 兼容模式（Compatible Mode）→ 丢弃异常行 → 继续处理
```

**分析**：exBase 的冲突处理更加**细粒度**，支持按冲突类型分别配置策略，且提供手动干预能力。RTSync 的处理更加**简洁粗放**，但通过严格/兼容两种模式覆盖了大部分场景。

---

## 13. 监控与运维对比

| 能力 | exBase | RTSync |
|------|--------|--------|
| **任务状态监控** | ✓ | ✓ |
| **同步进度可视化** | ✓ | ✓ |
| **同步速率展示** | ✓ | ✓ |
| **CDC 延迟监控** | - | ✓（cdc-lag-monitor） |
| **监控事件生命周期** | - | ✓（雁过留声） |
| **归因分析** | - | ✓（上下文+统计学分析） |
| **修复指引** | - | ✓（错误栈匹配知识库） |
| **CDC 配置诊断** | - | ✓（表级日志等级/磁盘空间检查） |
| **报错收敛/归一化** | - | ✓（top-error-monitor） |
| **通知策略配置** | - | ✓（前台可配置） |
| **指标生态集成** | - | ✓（导出指标到外部系统） |
| **作业日志管理** | ✓ | ✓（按对象名合并，展示最新记录） |
| **异常信息查询** | ✓ | ✓ |

**分析**：RTSync 构建了完整的三层监控体系（监视器 → 归因分析 → 通知策略），远超 exBase 的基础监控能力。这与两者的运行模式有关：RTSync 设计为长期运行的基础设施，因此需要更强的运维能力；exBase 是项目制工具，通常有专人跟进，对自动化运维需求较低。

---

## 14. 部署架构与高可用对比

| 能力 | exBase | RTSync |
|------|--------|--------|
| **标准部署** | ✓ | ✓ |
| **Docker 部署** | - | ✓（Playground 模式） |
| **高可用部署** | - | ✓（故障转移+负载均衡） |
| **故障转移（Failover）** | - | ✓（CDC Failover 策略） |
| **负载均衡** | - | ✓ |
| **数据库高可用** | 依赖目标库自身 HA | ✓（支持故障转移+负载均衡） |
| **最低浏览器要求** | - | Chrome 88 |
| **快速体验/评估** | - | ✓（Docker Playground） |
| **许可证管理** | 商业授权 | ✓（在线更新） |
| **环境要求** | JDBC 可达即可 | 可访问公网（Playground）或私有部署 |

**分析**：RTSync 在高可用和部署灵活性方面有显著优势。exBase 作为迁移工具，通常部署在迁移实施期间，对高可用的需求相对较低。

---

## 15. 性能特性对比

| 能力 | exBase | RTSync |
|------|--------|--------|
| **作业并行度配置** | ✓（可调） | ✓（读写端并发可调） |
| **全量迁移性能** | 批量导入 | 数据库快照+并行读写 |
| **增量同步性能** | Kafka 缓存提升 | 流批融合架构（60% 提升） |
| **写入优化** | JDBC 批量写入 | Load Data/两阶段提交/HttpServer Load |
| **性能基准测试工具** | - | ✓（Dummy 连接器+Netperf） |
| **CBO+HBO 自动调优** | - | ✓ |
| **内存优化** | - | ✓（Kafka 连接器/任务调度优化） |
| **对源库影响** | JDBC 连接，通过 Kafka 缓存减压 | 日志解析，几乎零影响 |

### 15.1 性能参考数据

**RTSync(Bluepipe) 用户反馈**：
> "Oracle 里 9000 张表，120GB 左右；它一台机器，连带自动建表和数据校验，一小时多一点就全复制过去了。"

**exBase**：
> 迁移完成后展示迁移时间、耗时信息，并给出修改窗口的手工修改入库时间。

---

## 16. 适用场景对比

### 16.1 exBase 最佳适用场景

| 场景 | 适配度 | 说明 |
|------|--------|------|
| **Oracle → Vastbase 迁移** | ★★★★★ | 核心场景，规则模板最完善 |
| **信创/去O 数据库替代项目** | ★★★★★ | 评估+转换+迁移+校验全流程 |
| **大型遗留系统迁移** | ★★★★★ | PL/SQL 自动转换极大降低工作量 |
| **迁移可行性评估** | ★★★★★ | 独有的兼容性分析和工作量预估 |
| **应用 SQL 适配改造** | ★★★★ | 自动采集+分析+改造建议 |
| **带回切保障的迁移** | ★★★★ | 正反向增量同步 |
| **持续数据实时同步** | ★★ | 非核心定位，能力有限 |
| **多库间数据共享** | ★ | 非设计目标 |

### 16.2 RTSync(Bluepipe) 最佳适用场景

| 场景 | 适配度 | 说明 |
|------|--------|------|
| **异构数据库持续实时同步** | ★★★★★ | 核心场景，23+ 连接器 |
| **实时数据仓库构建** | ★★★★★ | CDC → Doris/Hive |
| **金融交易系统实时备份** | ★★★★★ | 毫秒级延迟+断点续传 |
| **微服务数据同步** | ★★★★ | Kafka 集成 |
| **数据库国产化迁移（数据层）** | ★★★★ | 全量+增量+比对 |
| **测试环境数据初始化** | ★★★★ | TPC-H/TPC-DS 数据生成 |
| **多活/灾备数据同步** | ★★★★ | 高可用+故障转移 |
| **迁移前可行性评估** | - | 不支持 |
| **PL/SQL 代码改造** | - | 不支持 |

### 16.3 场景互补分析

在实际的数据库国产化替代项目中，exBase 和 RTSync 可以**互补使用**：

```
阶段一（评估规划）：exBase 进行兼容性评估，生成分析报告
         ↓
阶段二（结构迁移）：exBase 进行 DDL/PL-SQL 转换，在目标库重建对象
         ↓
阶段三（数据迁移）：exBase 或 RTSync 进行全量数据迁移
         ↓
阶段四（增量同步）：RTSync 开启 CDC 实时复制，保持数据同步
         ↓
阶段五（数据验证）：RTSync 持续数据比对，确保一致性
         ↓
阶段六（业务割接）：切换业务至新库
         ↓
阶段七（持续同步）：RTSync 继续运行，保障多库间数据一致
```

---

## 17. 产品成熟度与生态对比

| 维度 | exBase | RTSync |
|------|--------|--------|
| **产品历史** | 多年，配合 Vastbase 生态 | 从 v2.1(2024.11) 至 v2.14(2026.03)，14 个版本 |
| **版本迭代频率** | 中等 | 高（约每月一版） |
| **文档完善度** | 官方知识中心 + 智库系列文章 | 完整 Docusaurus 文档站 |
| **社区资源** | 墨天轮、腾讯云等技术社区文章 | 较少公开资料 |
| **生态绑定** | 深度绑定 Vastbase 生态 | 独立中立，不绑定特定数据库 |
| **行业案例** | 政务、金融、能源等（公开案例） | Oracle 迁移案例（用户反馈） |
| **厂商背景** | A 股上市公司（603138） | 初创/成长期企业 |
| **产品白皮书** | ✓（V3.12 产品/技术白皮书） | - |

---

## 18. 选型建议

### 18.1 选 exBase 的场景

- 核心目标是将 **Oracle/DB2/SQL Server 迁移到 Vastbase/openGauss**
- 需要在迁移前进行**全面的兼容性评估和工作量预估**
- 系统有大量**存储过程、函数、触发器**等 PL/SQL 对象需要转换
- 项目是一次性的**数据库替代工程**，有明确的启动和结束时间
- 需要**反向增量同步**保障迁移回切安全
- 已经在使用 Vastbase 产品生态，希望获得一体化方案

### 18.2 选 RTSync(Bluepipe) 的场景

- 核心目标是建立**长期运行的跨库实时数据同步**
- 需要在**多种异构数据库**之间灵活地流转数据（20+ 种连接器）
- 对同步**延迟要求高**（毫秒级 CDC）
- 需要**持续的数据一致性监控**（自动比对 + MD5 全文比对）
- 部署环境需要**高可用保障**（故障转移 + 负载均衡）
- 需要构建**实时数据仓库**或**实时备份**系统
- 需要将数据同步能力通过 **API/SDK 集成**到现有系统
- 需要快速**评估验证**（Docker Playground 模式）

### 18.3 联合使用的场景

- 大型信创/去O 项目，需要**评估 + 迁移 + 持续同步**的全流程能力
- 分阶段迁移项目：exBase 负责评估和结构迁移，RTSync 负责数据同步和持续保障
- 迁移完成后仍需保持新旧库**双向数据同步**一段过渡期

---

## 19. 总结

### 19.1 一句话总结

> **exBase 是"搬家公司"，RTSync 是"物流管道"。**
>
> exBase 帮你把数据库从旧家（Oracle）搬到新家（Vastbase），搬之前帮你评估房子能不能住、家具能不能放下、搬家要多久多少钱；搬完还帮你检查有没有东西落下。搬完就撤。
>
> RTSync 是在两个或多个家之间建设的永久性输送管道，保证数据实时、持续、准确地在各处流转。管道一旦建成就长期运行，持续监控数据有没有漏。

### 19.2 关键差异总结

| 维度 | exBase | RTSync(Bluepipe) |
|------|--------|------------------|
| **核心价值** | 降低迁移风险和成本 | 保障数据实时一致性 |
| **使用周期** | 项目制（有限周期） | 持续运行（基础设施） |
| **技术壁垒** | DDL/PL-SQL 智能转换 | 流批融合 + 23 连接器 |
| **最独特能力** | 迁移评估 + 反向增量 | 双模式数据比对 + 监控体系 |
| **目标用户** | DBA / 迁移项目团队 | 数据工程师 / 运维团队 |
| **竞品对标** | Ora2Pg, ADAM, DBMT | Debezium, CloudCanal, OMS |

### 19.3 能力雷达图（定性评估）

```
                    迁移评估
                      5
                     ╱ ╲
                   4╱   ╲4
         SQL转换  3╱─────╲3  数据库覆盖
                 2╱   ╱╲  ╲2
                1╱  ╱    ╲  ╲1
    全量迁移  ───╱─╱──────╲─╲───  实时同步
                 ╲╱        ╲╱
                 2╲        ╱2
                 3 ╲      ╱ 3
         冲突处理 4 ╲    ╱ 4  数据比对
                  5  ╲  ╱  5
                      ╲╱
                    监控运维

        ─── exBase     ─── RTSync
```

| 维度 | exBase | RTSync |
|------|--------|--------|
| 迁移评估 | 5 | 0 |
| SQL 转换 | 5 | 1 |
| 全量迁移 | 4 | 5 |
| 冲突处理 | 5 | 3 |
| 监控运维 | 2 | 4 |
| 数据比对 | 3 | 5 |
| 实时同步 | 3 | 5 |
| 数据库覆盖 | 3 | 5 |

两款产品各有所长，**不构成直接替代关系**，而是在数据库国产化替代的不同阶段和不同场景中发挥互补作用。企业应根据自身的核心诉求——是"一次性迁移"还是"持续同步"——做出合理选型，或在大型项目中联合使用以发挥各自优势。

---

*本报告基于公开可获取的产品文档和技术资料编制，力求客观公正。具体产品能力请以厂商最新版本文档为准。*

*版权所有 &copy; 2026. All Rights Reserved.*
